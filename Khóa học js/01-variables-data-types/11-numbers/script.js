let x;

const num = new Number(5);

// toString() - trả về biểu diễn chuỗi của số
x = num.toString();
// // Để lấy chiều dài
// x = num.toString().length;

// // toFixed() - trả về biểu diễn chuỗi của số với số lẻ cố định
// x = num.toFixed(2);

// // toPrecision() - trả về một số với chiều dài xác định
// x = num.toPrecision(3);

// // toExponential() - chuyển đổi số thành biểu diễn số mũ và trả về giá trị dưới dạng chuỗi
// x = num.toExponential(2);

// // toLocaleString() - trả về biểu diễn chuỗi của số, sử dụng ngôn ngữ hiện tại
// x = num.toLocaleString('en-US');

// // valueOf - Lấy giá trị
// x = num.valueOf();

// Chính đối tượng Number cũng có một số thuộc tính và phương thức

// Số lớn nhất và nhỏ nhất có thể
// x = Number.MAX_VALUE;
// x = Number.MIN_VALUE;

console.log(x);
